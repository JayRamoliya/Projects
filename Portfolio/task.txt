setup project : npm create vite@latest

clean all files

change title Portfolio

install styled components : npm install styled-components
install react icon : npm install react-icons
install css animate library : npm install animate.css

dawnload all images


1. create a Components folder in src

2.  - Navbar.jsx components inside Components folder
    - import Navbar.jsx in App.jsx file
    - first import styled components
    - import Poppins fonts
    - import three icon 
    - create a Main and Right styled components

3.  - HelloPage.jsx components inside Components folder
    - import HelloPage.jsx in Navbar.jsx file
    - first import styled components
    - create a Main styled components

4.  - AboutPage.jsx components inside Components folder
    - import AboutPage.jsx in App.jsx file
    - first import styled components
    - create a Main & Container styled components
    - create a data array for store some value 
    - write a map function on array 
    - import two react icon

5.  - install react router dom : npm install react-router-dom

6.  - SkillPage.jsx components inside Components folder
    - import SkillPage.jsx in App.jsx file
    - first import styled components
    - create a Main & Container styled components
    - create a image array for store all image
    - write a map function on array

7.  - install gsap : npm install gsap
